# Domain modules

Each business capability lives in its own NestJS module. Current modules are `auth`, `users`, and `leads`; new capabilities should follow the same module boundary with controllers, services, schemas, DTOs, and tests colocated inside the module.
